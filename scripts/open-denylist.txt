# Never export these even if under an allowlisted prefix.
# Matching rules (export-open-src.ps1 Test-Denied):
#   - exact path match, OR
#   - path PREFIX at a segment boundary (e.g. "runtime/" → runtime\** only)
# Do NOT use bare substrings — "runtime" must not match src/main/runtime.js

# Private product / lab (top-level dirs)
runtime/
release/
reference/
AGENTS.md

# Build / local
.git/
dist/
export/
node_modules/
.omc/
.workbuddy/
ida-out/
